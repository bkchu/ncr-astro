---
export interface Props {
  links: {
    title: string;
    href: string;
  }[];
}

const { links } = Astro.props;
---

<nav>
  <ul>
    {
      links.map((link) => (
        <li>
          <a href={link.href}>{link.title}</a>
        </li>
      ))
    }
  </ul>
</nav>
